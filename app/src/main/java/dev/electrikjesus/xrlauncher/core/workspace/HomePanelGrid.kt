package dev.electrikjesus.xrlauncher.core.workspace

/**
 * 2D app grid on the Home pane — shows Desktop-placed apps only
 * (distinct from the sphere All Apps drawer).
 */
object HomePanelGrid {
    const val COLS = 4
    const val ROWS = 3
    const val PAGE_SIZE = COLS * ROWS
}
